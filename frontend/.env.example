VITE_API_BASE_URL=http://localhost:4000

# Must exactly match DASHBOARD_API_KEY in backend/.env. Leave both blank for
# local dev — auth is only enforced once a key is set on the backend.
VITE_API_KEY=
